{
  "registry_id": "packauth:registry:environment",
  "registry_version": "1.0.0",
  "concept": "environment_variable",
  "note": "Client projection of packauth:registry:environment, generated by scripts/build-openapi.mjs. Carries the variables a client reads; the full registry lives in the PackAuth repository.",
  "variables": [
    {
      "name": "PACKAUTH_TOKEN",
      "concept": "credential",
      "side": "client",
      "required": true,
      "aliases": [
        "PACKAUTH_API_KEY"
      ],
      "plane_bound": true,
      "description": "The tenant API key. `pa_live_` on production, `pa_test_` on the sandbox plane. Sent as `Authorization: Bearer`.",
      "consumers": [
        "sdk/src/environment.mjs",
        "sdk-python/packauth/__init__.py",
        "tools/packauth-corpus-mcp/test/self-test.mjs"
      ]
    },
    {
      "name": "PACKAUTH_API_URL",
      "concept": "api_base_url",
      "side": "client",
      "required": false,
      "default": "https://api.packauth.com",
      "aliases": [
        "PACKAUTH_API_BASE",
        "PACKAUTH_BASE"
      ],
      "plane_bound": true,
      "description": "Which PackAuth to talk to. Unset means production, which is the safe default for a client and the dangerous one for a developer who meant to use the sandbox — hence the plane check.",
      "consumers": [
        "sdk/src/environment.mjs"
      ]
    }
  ],
  "plane_prefixes": [
    {
      "plane": "production",
      "key_prefix": "pa_live_",
      "host_suffix": "api.packauth.com"
    },
    {
      "plane": "sandbox",
      "key_prefix": "pa_test_",
      "host_suffix": "sandbox.packauth.com"
    }
  ]
}
